# Private Camera Extension

A per-player camera system for MakeCode Arcade multiplayer.

## Features
- Each player gets their own camera (per device)
- Smooth camera movement
- Deadzone support
- Automatic player setup

## Usage

```ts
privatecamera.start()
